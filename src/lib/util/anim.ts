import { linear } from 'svelte/easing';

export const fadeIn = {
    delay: 0,
    duration: 300,
    easing: linear
};

export const fadeOut = {
    delay: 100,
    duration: 300,
    easing: linear
};